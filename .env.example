# ============================================================================
# Pixel Buddy Environment Configuration
# ============================================================================

# ----------------------------------------------------------------------------
# LOCAL DEVELOPMENT (Docker)
# ----------------------------------------------------------------------------
# These settings work with docker-compose.yml
# Just copy this file to .env and start with: ./scripts/dev.sh up

# Database (Docker)
DATABASE_URL=postgresql://postgres:pixel_secret_2025@db:5432/pixel_buddy
DB_PASSWORD=pixel_secret_2025
DB_PORT=5432

# Server
PORT=3000
APP_PORT=3000
NODE_ENV=development

# Security
CORS_ORIGIN=*

# AI (Ollama) - now enabled by default in docker-compose.yml
OLLAMA_URL=http://ollama:11434
OLLAMA_PORT=11434
OLLAMA_MEMORY_LIMIT=4G
OLLAMA_MEMORY_RESERVE=2G
OLLAMA_NUM_PARALLEL=2
OLLAMA_MAX_QUEUE=128
OLLAMA_KEEP_ALIVE=5m

# ----------------------------------------------------------------------------
# RAILWAY DEPLOYMENT
# ----------------------------------------------------------------------------
# When deploying to Railway, these variables are auto-provided:
# DATABASE_URL=${{Postgres.DATABASE_URL}}
# OLLAMA_URL=${{Ollama.OLLAMA_URL}}
# PORT=${{PORT}}

# For Railway, set these manually:
# NODE_ENV=production
# CORS_ORIGIN=https://yourdomain.com

# ----------------------------------------------------------------------------
# NATIVE LOCAL (without Docker)
# ----------------------------------------------------------------------------
# If running without Docker, use these instead:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/pixel_buddy
# OLLAMA_URL=http://localhost:11434
